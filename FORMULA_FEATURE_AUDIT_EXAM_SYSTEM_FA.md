# ممیزی دقیق ویژگی‌ها و قابلیت‌های بخش فرمول `exam-system.html`

**تاریخ ممیزی:** ۲۰۲۶-۰۸-۱۳
**روش:** تحلیل ایستا؛ هیچ JavaScript از فایل اجرا نشد.
**فایل:** `/home/user/uploads/exam-system.html`
**SHA-256:** `591dd9feffecea45fe423633643cd518c1db9cad547bbfda709ad93f2cd7e6a1`
**اندازه:** `1,540,210` بایت، `32,099` خط
**بررسی نحوی:** همهٔ scriptهای inline با `node --check` بدون خطای syntax بودند.

## ۱) نتیجهٔ اجرایی

این فایل یک ویرایشگر فرمول نسبتاً گسترده و اختصاصی دارد که از سه لایه تشکیل شده است:

1. **مدل ساختاری جعبه‌ای** برای ویرایش؛
2. **مبدل دوطرفهٔ مدل جعبه‌ای ↔ TeX**؛
3. **موتور TeX محدود → HTML/CSS** برای نمایش در سازنده، دانش‌آموز، تصحیح و چاپ.

نکتهٔ مهم: این موتور **MathJax/KaTeX نیست** و تقریباً همهٔ فرمول را با HTML، CSS و glyph متنی می‌سازد. فقط شکل رادیکال از SVG استفاده می‌کند. بنابراین عبارت «همهٔ نمادها SVG هستند» دربارهٔ این فایل درست نیست.

از نظر داده، بخش فرمول شامل این موارد است:

```text
گروه اصلی موضوعی                         8
پیوند گروه به دسته                       75
دستهٔ ثابت                               77
تب runtime با افزودن «اخیر»              78
ورودی کتابخانه                         2084
ورودی Unicode                           1200
گروه گالری آماده                           5
فرمول گالری آماده                         34
درس Smart Hub                              5
قالب درس‌محور Smart Hub                   30
بستهٔ امتحانی Smart Hub                    8
جفت دلیمتر Smart Hub                       6
کلید درشت Smart Hub                        8
```

با این حال، چند قابلیت مهم فقط در کد وجود دارند و در رابط عادی قابل دسترسی نیستند. مهم‌ترین آن‌ها حالت تایپ سریع، گالری، جست‌وجوی اصلی، نوار Undo/Copy و Smart Hub است.

## ۲) مقایسه با مرجع قبلی

بخش‌های اصلی فرمول این فایل با فایل قبلی `index.html` یکسان‌اند:

```text
HTML مودال فرمول: byte-identical
mathedit.js:       byte-identical
math.js:           byte-identical
```

در فایل جدید، bootstrap برای بارگذاری تنبل `mathedit.js` اضافه شده، اما خود کد کامل `mathedit.js` همچنان داخل HTML قرار دارد. در نتیجه «بارگذاری تنبل واقعی» در همین فایل عملاً رخ نمی‌دهد و مرورگر همچنان کل کد فرمول را هنگام بارگذاری سند parse می‌کند.

## ۳) قابلیت‌های واقعاً قابل مشاهده در اجرای عادی

`openMath()` همیشه کلاس `box-fullscreen` را اضافه می‌کند. در این وضعیت، رابط عملی و قابل مشاهده شامل این قسمت‌هاست:

1. بوم جعبه‌ای فرمول؛
2. ردیف دوطبقهٔ گروه‌های کتابخانه؛
3. ردیف شش‌تایی «اخیر / تبدیل / log / انتگرال / درصد / sin»؛
4. ابزارهای «درج / سطر تازه / abc / کسر / توان / رادیکال»؛
5. keypad چهارردیفی و شش‌ستونه؛
6. کتابخانه‌های تمام‌صفحه‌ای که از گروه‌ها باز می‌شوند.

### امکاناتی که در HTML وجود دارند ولی در حالت fullscreen پنهان می‌شوند

| قابلیت | در کد وجود دارد | در اجرای عادی قابل دسترسی است؟ | علت |
|---|---:|---:|---|
| حالت جعبه‌ای | بله | بله | pane اصلی fullscreen |
| حالت تایپ سریع | بله | خیر | `.mf-modes` پنهان و `mfMode('box')` اجباری است |
| گالری آماده | بله | خیر | دکمهٔ mode پنهان است |
| راهنمای بالا | بله | خیر | `.mf-help` پنهان است |
| Undo/Redo/Copy/Paste/Zoom | بله | دکمه‌ها خیر | `.mb-tools` پنهان؛ بخشی از میانبرها کار می‌کنند |
| جست‌وجوی نماد | بله | خیر | `.mb-symbol-search` پنهان است |
| تب‌های مستقیم کتابخانه | بله | خیر | `#mfTabs` و `#mfPad` در حالت عادی پنهان‌اند |
| کد حرفه‌ای TeX | بله | خیر | `.mf-code` پنهان است |
| footer درج/پاک/انصراف | بله | خیر | `.mf-act` پنهان است |
| Smart Hub | بله | خیر | هیچ دکمه یا فراخوانی برای `mbOpenSmartHub()` وجود ندارد |

در fullscreen، درج از دکمهٔ «درج» بالای keypad و پاک‌کردن از کلید `C` انجام می‌شود. دکمهٔ انصراف و سربرگ بستن نیز پنهان‌اند؛ مسیر صریح باقی‌مانده برای خروج بدون درج، کلید `Esc` است.

## ۴) مدل ساختاری ویرایشگر جعبه‌ای

مدل مرکزی `MB` این وضعیت‌ها را نگه می‌دارد:

```text
root       درخت/آرایهٔ اصلی فرمول
cur        مکان‌نمای ساختاری {arr, i}
reg        نگاشت شناسهٔ slot به آرایهٔ آن
info       رابطهٔ slot با node والد
order/tab  ترتیب پیمایش
rects      مختصات واقعی slotها برای ناوبری مکانی
hist       Undo
future     Redo
zoom       بزرگ‌نمایی
```

### نوع گره‌های پشتیبانی‌شده

1. `c` — حرف یا کاراکتر عادی؛
2. `sym` — نماد ریاضی؛
3. `fn` — تابع مانند sin/log؛
4. `frac` — کسر؛
5. `sup` — توان؛
6. `sub` — زیرنویس؛
7. `sqrt` — رادیکال، با فرجهٔ اختیاری؛
8. `par` — پرانتز، کروشه، آکولاد، قدرمطلق، کف و سقف؛
9. `mixed` — عدد مخلوط؛
10. `op` — عملگر بزرگ با حد بالا/پایین؛
11. `vec` — بردار؛
12. `bar` — خط بالا؛
13. `hat` — کلاه؛
14. `fact` — فاکتوریل؛
15. `matrix` — ماتریس؛
16. `text` — متن عادی داخل فرمول؛
17. `br` — سطر تازه.

### رفتار جعبه‌ها

- هر ساختار فرزند slotهای مستقل دارد؛ مانند صورت/مخرج، توان، زیرنویس، فرجه و بدنهٔ رادیکال، حدهای عملگر و سلول ماتریس.
- slot خالی با کادر dashed دیده می‌شود.
- slot فعال رنگ و outline می‌گیرد.
- در fullscreen، slot پرشده و غیرفعال عمداً بدون border و background نمایش داده می‌شود؛ بنابراین همهٔ قسمت‌های قابل ویرایش هم‌زمان به شکل جعبه دیده نمی‌شوند.
- root در fullscreen هیچ border فعالی ندارد.
- نشانگر درج با `mb-cur` چشمک می‌زند.
- ورودی واقعی صفحه‌کلید یک input شفاف `1×1px` است.

### درج هوشمند هنگام تایپ

- `/` مقدار سمت چپ را به صورت کسر تبدیل و مخرج را فعال می‌کند.
- `^` توان و `_` زیرنویس می‌سازد.
- `(` ساختار پرانتز می‌سازد و `)` از گروه خارج می‌شود.
- `*` به `×` تبدیل می‌شود.
- تایپ `->` یا `<-` فلش مناسب می‌سازد.
- Enter در input مخفی فرمول را درج می‌کند؛ دکمهٔ `↵` داخل فرمول سطر تازه می‌سازد.

## ۵) ناوبری و ویرایش

### ناوبری

- چپ/راست داخل درخت حرکت می‌کند و هنگام رسیدن به node ساختاری وارد slotهای آن می‌شود.
- بالا/پایین با استفاده از `getBoundingClientRect` نزدیک‌ترین slot مکانی را پیدا می‌کند.
- Tab/Shift+Tab بین slotها به‌صورت چرخه‌ای حرکت می‌کند.
- Home/End ابتدا/انتهای slot فعلی را انتخاب می‌کند.
- پس از هر تغییر، مکان‌نما به‌صورت خودکار در بوم scroll می‌شود.
- کیبورد دستگاه در fullscreen فقط با دکمهٔ `⌨` باز می‌شود.

### Undo/Redo

- حداکثر ۸۰ snapshot ذخیره می‌شود.
- مسیر ساختاری مکان‌نما برای بازیابی ثبت می‌شود.
- محتوای ماتریس restore می‌شود، اما مسیر مکان‌نمای داخل سلول ماتریس با ساختار `m0_0` با مدل `cells[][]` سازگار نیست؛ بنابراین بعد از Undo/Redo در ماتریس، مکان‌نما ممکن است به انتهای root برگردد.
- `mbSlotsOf()` سلول‌های ماتریس را برنمی‌گرداند؛ در نتیجه حرکت افقی/Backspace روی node ماتریس مانند ساختارهای دیگر عمل نمی‌کند.

### میانبرها

```text
Ctrl/Cmd+Z             Undo
Ctrl/Cmd+Shift+Z       Redo
Ctrl/Cmd+Y             Redo
Ctrl/Cmd+C             Copy TeX
Ctrl/Cmd+V             paste مرورگر؛ TeX در event paste پردازش می‌شود
Backspace              حذف/ورود به آخرین slot ساختار
Arrow Left/Right       حرکت ساختاری
Arrow Up/Down          حرکت مکانی یا Tab fallback
Tab / Shift+Tab        slot بعدی/قبلی
Home / End             ابتدا/انتهای slot
Enter                  درج فرمول در سؤال
Esc                    اول popup و سپس ویرایشگر را می‌بندد
```

## ۶) کپی، پیست و Import

- Copy فقط TeX خام را بدون `$` در clipboard قرار می‌دهد.
- Paste از Clipboard API استفاده می‌کند و fallback خواندن ندارد.
- `$`های ابتدا/انتها از متن پیست‌شده حذف می‌شوند.
- اگر متن هیچ فرمان `\command` نداشته باشد، از مبدل طبیعی عبور می‌کند.
- پیست toolbar، کل مدل فعلی را جایگزین می‌کند؛ داخل slot فعال درج نمی‌شود.
- paste مستقیم در input مخفی فقط برای TeX یا متن دارای `$` intercept می‌شود؛ متن ساده حرف‌به‌حرف وارد می‌شود.

## ۷) رندر فرمول

### ماهیت رندر

رندر اصلی HTML/CSS است:

- حروف و نمادها: glyph متنی در `<span>`؛
- کسر: Flexbox + border؛
- توان/زیرنویس: `<sup>/<sub>`؛
- ماتریس: `<table>` + borderهای CSS؛
- دلیمترها: نویسهٔ Unicode با `scaleY`؛
- بردار/کلاه/خط بالا: CSS؛
- رادیکال: علامت SVG + خط بالای CSS.

فقط رادیکال و آیکون رادیکال toolbar SVG هستند. باقی نمادها SVG نیستند.

### رادیکال کش‌پذیر

- تیک و شیب رادیکال از `SURD_HTML` ساخته می‌شود.
- ارتفاع SVG بر اساس ارتفاع واقعی `.msqrt` تنظیم می‌شود.
- انتهای مسیر SVG روی border بالای `.mrad` هم‌پوشانی دارد.
- خط افقی اصلی رادیکال، `border-top: 2.8px` روی `.mrad` است و با عرض محتوای داخل ریشه خودکار کش می‌آید.
- پس از draw، zoom و هر Mutation در کل `document.body` دوباره fit می‌شود.

### دلیمترها

- `()`, `[]`, `{}`, `| |`, کف، سقف و `⟨⟩` پشتیبانی می‌شوند.
- پرانتز/کروشه/آکولاد با glyph واقعی و scale عمودی تا حدود ۴٫۶ برابر کشیده می‌شوند.
- کف/سقف با borderهای CSS ساخته می‌شوند.
- ماتریس صرف‌نظر از environment ورودی، عملاً با کروشهٔ مربعی نمایش داده می‌شود.

### ساختارهای TeX قابل نمایش

- `frac/dfrac/tfrac` و `sfrac/nicefrac`؛
- `sqrt` و `root`؛
- scripts؛
- `left/right`؛
- floor/ceil/brace؛
- overline/underline/vector/hat/widehat؛
- text/mathrm/mbox و bold؛
- matrix environment؛
- عملگرهای بزرگ و حد بالا/پایین؛
- توابع مثلثاتی، لگاریتم، hyperbolic، det/gcd/mod؛
- مجموعهٔ گسترده‌ای از روابط، فلش‌ها و حروف یونانی؛
- فاصله‌های `quad/qquad`.

فرمان ناشناخته به‌صورت کد قرمز `\command` نمایش داده می‌شود؛ بنابراین موتور fallback تصویری ندارد.

### اختلاف parser نمایش و parser جعبه‌ای

موتور نمایش از فرمان‌هایی مانند `sfrac`, `nicefrac`, `root`, `underline`, `widehat`, `quad` و `qquad` پشتیبانی می‌کند، اما parser جعبه‌ای همهٔ آن‌ها را به node ساختاری تبدیل نمی‌کند. در Import/Edit این فرمان‌ها ممکن است به متن خام تبدیل شوند و round-trip کامل نباشد.

## ۸) حالت تایپ سریع

حالت تایپ سریع در کد قابلیت‌های زیر را دارد:

- اعداد فارسی/عربی و لاتین؛
- کسر با `/`؛
- توان و زیرنویس؛
- گروه‌بندی پرانتزی؛
- `>=`, `<=`, `!=`, `+-`, `->`, `=>`, `~=`؛
- `*` → ضرب و `÷`؛
- واژه‌های فارسی و انگلیسی برای رادیکال، یونانی، مجموع، انتگرال، حد و توابع؛
- تبدیل واژهٔ ناشناختهٔ چندحرفی به `\text{...}`؛
- انتقال نتیجه به مدل جعبه‌ای.

نمونه‌های اعلام‌شده در UI:

```text
7/8
x^2
x^2^3
sqrt2
رادیکال ۵
(a+b)/2
pi
>=
!=
*
```

این mode در اجرای fullscreen دکمهٔ قابل مشاهده ندارد. فقط Quick Convert ردیف شش‌تایی، بخشی از همین قابلیت را به‌شکل قابل دسترسی ارائه می‌کند.

### تبدیل هوشمند شیمی

کد Smart Hub تلاش می‌کند:

- اعداد فرمول شیمی را زیرنویس کند؛
- بار یون را توان بالا کند؛
- یون‌های رایج را normalize کند؛
- فلش و تعادل را تبدیل کند.

ایراد قطعی: `⇌` به متن `rightleftharpoons` بدون backslash تبدیل می‌شود؛ `natToTex` آن را فرمان TeX نمی‌شناسد و احتمالاً به `\text{rightleftharpoons}` تبدیل می‌کند، نه `\rightleftharpoons`.

## ۹) گالری آماده

گالری ثابت ۳۴ فرمول دارد:

| گروه | تعداد |
|---|---:|
| هندسه | 11 |
| جبر | 10 |
| مثلثات | 4 |
| آمار و نسبت | 5 |
| آنالیز | 4 |

- جست‌وجو روی نام و TeX انجام می‌شود.
- حداکثر ۸ فرمول اخیر به ابتدای گالری اضافه می‌شود.
- انتخاب گالری کل مدل فعلی را جایگزین می‌کند و به mode جعبه‌ای می‌رود.
- mode گالری در fullscreen عادی قابل دسترسی نیست.

## ۱۰) کتابخانهٔ نمادها و فرمول‌ها

### گروه‌های اصلی

| کلید | گروه | تعداد دسته |
|---|---|---:|
| `num` | اعداد و محاسبات | 16 |
| `an` | آنالیز و توابع | 12 |
| `alg` | جبر و معادلات | 7 |
| `tri` | مثلثات و یونانی | 4 |
| `set` | مجموعه و منطق | 5 |
| `geo` | هندسه و بردار | 7 |
| `phys` | فیزیک | 14 |
| `chem` | شیمی | 10 |

### رفتار کتابخانه

- گروه → زیرگروه → کتابخانهٔ تمام‌صفحه؛
- preview با `mathToHtml`؛
- موارد Unicode خام بدون تبدیل TeX؛
- variant menu برای کسر، توان، رادیکال و دلیمتر؛
- حروف کوچک/بزرگ؛
- ماتریس آماده و ماتریس دلخواه ۱ تا ۱۰ سطر/ستون؛
- dedup بر اساس label + tex + action؛
- انتخاب عادی در محل cursor فعلی insert می‌شود؛
- recent/quick-convert/gallery/paste کل فرمول را جایگزین می‌کنند؛
- long-press مورد را به علاقه‌مندی می‌افزاید یا حذف می‌کند.

### Unicode 1200

داده واقعاً ۱۲۰۰ ورودی Unicode دارد، اما `mbOpenUnicodeLibrary()` نمادهایی را که به دسته‌های موضوعی منتقل شده‌اند فیلتر می‌کند. با اعمال عین قواعد classification فایل:

```text
کل داده Unicode             1200
منتقل/فیلترشده              1061
قابل مشاهده در پنجره Unicode حدود 139
```

بنابراین عنوان «Unicode (1200)» تعداد دادهٔ کل را می‌گوید، نه تعداد مواردی که در همان پنجره نمایش داده می‌شوند. نمادهای حذف‌شده قرار است در دسته‌های موضوعی قابل دسترسی باشند.

### جست‌وجو

- روی label و TeX همهٔ دسته‌ها جست‌وجو می‌کند.
- نتیجه deduplicate می‌شود.
- نام رسمی Unicode انگلیسی قابل جست‌وجو است.
- خود input جست‌وجو در fullscreen پنهان شده است؛ بنابراین این قابلیت در مسیر عادی قابل استفاده نیست.

## ۱۱) علاقه‌مندی و سابقه

| داده | کلید localStorage | سقف |
|---|---|---:|
| فرمول‌های اخیر | `mf_recent` | عملاً 8 |
| نمادهای اخیر | `mb_recent_symbols_v1` | 24 |
| علاقه‌مندی‌ها | `mf_fav_formulas_v1` | 60 |
| آخرین فرمول | `mf_last_formula_v989` | 1 |

- ستاره‌ها در CSS مخفی شده‌اند؛ افزودن/حذف با نگه‌داشتن انجام می‌شود.
- کد واقعی long-press روی ۱۰۰۰ms تنظیم شده است.
- متن راهنما و title می‌گویند «۲ ثانیه»؛ این دو با رفتار واقعی ناسازگارند.
- recent formula امکان انتخاب و حذف دارد.
- انتخاب recent formula کل فرمول فعلی را جایگزین می‌کند.

## ۱۲) Smart Hub

Smart Hub در کد پیاده‌سازی قابل توجهی دارد:

- آخرین فرمول؛
- preview نهایی/خام؛
- علاقه‌مندی‌های سریع؛
- ۵ درس: ریاضی، هندسه، آمار، فیزیک، شیمی؛
- ۳۰ قالب درس‌محور؛
- Quick Convert با normalization شیمی؛
- ۸ مورد اخیر؛
- ۸ بستهٔ امتحانی؛
- ۶ جفت دلیمتر؛
- ۸ کلید درشت موبایل؛
- کتابخانهٔ همان درس.

اما فقط تعریف و export تابع `mbOpenSmartHub()` وجود دارد. هیچ button، onclick یا فراخوانی دیگری در فایل آن را اجرا نمی‌کند. در نتیجه Smart Hub در رابط فعلی **قابلیت نهفته و غیرقابل دسترسی** است.

## ۱۳) اتصال به سؤال و نمایش نهایی

### محل‌های درج

- متن سؤال؛
- گزینه‌های چندگزینه‌ای؛
- ستون راست و چپ جورکردنی.

### فرمت ذخیره

فرمول به‌صورت زیر در متن target درج می‌شود:

```text
$TeX$
```

درج در selection فعلی textarea انجام می‌شود. برای ویرایش یک فرمول قبلی، کاربر باید دقیقاً کل عبارت `$...$` را انتخاب کند و سپس ویرایشگر را باز کند؛ در غیر این صورت ویرایشگر خالی باز می‌شود و فرمول جدید درج خواهد شد.

### نمایش

- `renderMathText()` متن عادی را escape و بخش‌های بین `$...$` را رندر می‌کند.
- سؤال، گزینه، matching، پاسخ دانش‌آموز، تصحیح و چاپ از همین renderer استفاده می‌کنند.
- preview سازنده در یک div جدا بعد از textarea نمایش داده می‌شود؛ برخلاف نام کامنت، داخل خود textarea نیست.
- preview با `aria-hidden=true` از صفحه‌خوان پنهان است، ولی متن خام textarea همچنان در دسترس است.

## ۱۴) دسترس‌پذیری و Responsive

نقاط مثبت:

- modal دارای `role=dialog`, `aria-modal` و label است؛
- بیشتر دکمه‌ها title/aria-label دارند؛
- keyboard shortcuts گسترده‌اند؛
- دکمه‌ها touch-action مناسب دارند؛
- safe-area پایین موبایل رعایت شده؛
- کتابخانه و Smart Hub طرح تمام‌صفحه و responsive دارند؛
- جهت فرمول LTR و متن رابط RTL تفکیک شده است.

محدودیت‌ها:

- focus trap صریح برای modal وجود ندارد؛
- سربرگ و دکمهٔ بستن در fullscreen پنهان‌اند؛
- footer انصراف پنهان است؛
- نام نمادهای کتابخانهٔ تمام‌صفحه با `.mfk-n { display:none }` مخفی می‌شود؛ روی touch، title عملاً جایگزین مناسبی نیست؛
- search و mode switch در fullscreen پنهان‌اند؛
- Smart Hub دسترسی ندارد.

## ۱۵) کارایی و نگهداری

- `MutationObserver` کل `document.body` را برای fit دوبارهٔ رادیکال‌ها پایش می‌کند، نه فقط ناحیهٔ فرمول؛ این می‌تواند در صفحه‌های پرتحرک هزینه ایجاد کند.
- هر redraw مختصات همهٔ slotها، رادیکال‌ها، دلیمترها و scroll مکان‌نما را محاسبه می‌کند.
- کتابخانه‌ها virtualization ندارند و همهٔ buttonها را یک‌جا می‌سازند.
- کد کامل `mathedit.js` همچنان inline است؛ پس bootstrap جدید بارگذاری تنبل واقعی ایجاد نکرده است.
- سه تابع `mbOpenQuickConvert`, `mbCloseQuickConvert`, `mbRunQuickConvert` دوبار تعریف شده‌اند و نسخهٔ آخر نسخهٔ اول را override می‌کند.
- `mbFocus`, `mbShowDeviceKeyboard`, `closeMath`, `mbFavToggle`, `mbOpenItemLibrary` و `mbCloseSymbolLibrary` نیز بعداً wrapper/reassignment می‌شوند. این رفتار فعلاً عمدی است، ولی نگهداری و تست را سخت می‌کند.

## ۱۶) ایرادها و ناسازگاری‌های قطعی

### اولویت بالا

1. **حالت تایپ سریع و گالری در اجرای عادی قابل دسترسی نیستند.** `openMath` همیشه fullscreen است و mode controls با `display:none!important` مخفی‌اند.
2. **جست‌وجوی نماد قابل دسترسی نیست.** input جست‌وجو در fullscreen پنهان است.
3. **Smart Hub کاملاً ساخته شده ولی هیچ trigger ندارد.** تابع `mbOpenSmartHub()` هیچ فراخوانی ندارد.
4. **عنوان Unicode 1200 با نمایش مستقیم تطابق ندارد.** تقریباً ۱۳۹ مورد در پنجرهٔ Unicode باقی می‌ماند.
5. **دکمهٔ صریح انصراف/بستن در fullscreen دیده نمی‌شود.** خروج بدون درج به Esc یا رفتار میزبان وابسته است.

### اولویت متوسط

6. **فرمول‌ها و نمادها عموماً SVG نیستند.** فقط رادیکال SVG است؛ بقیه HTML/CSS/glyph هستند.
7. **زمان long-press ناسازگار است.** رفتار ۱ ثانیه، متن UI دو ثانیه.
8. **Smart Quick Convert برای `⇌` فرمان صحیح تولید نمی‌کند.** backslash حذف شده است.
9. **Undo/Redo مکان‌نمای سلول ماتریس را دقیق restore نمی‌کند.** مسیر `mR_C` با `cells[][]` ناسازگار است.
10. **parser نمایش و parser ویرایش ساختارهای یکسانی ندارند.** round-trip برخی فرمان‌ها ناقص است.
11. **ویرایش فرمول قبلی نیازمند انتخاب دقیق `$...$` است.** مسیر click-to-edit وجود ندارد.
12. **نام نمادها در کتابخانهٔ fullscreen مخفی است.** قابلیت کشف روی موبایل کاهش می‌یابد.

### بدهی فنی

13. بارگذاری تنبل mathedit در این فایل صوری است، زیرا bundle کامل هنوز inline است.
14. تعریف‌های تکراری Quick Convert و wrapperهای متعدد، رفتار نهایی را وابسته به ترتیب کد کرده‌اند.
15. MutationObserver رادیکال به کل body متصل است.
16. فرمان ناشناخته به‌صورت کد قرمز نمایش داده می‌شود، نه fallback دیداری.

## ۱۷) جمع‌بندی برای پیاده‌سازی Native

ویژگی‌های مرجع که واقعاً ارزش انتقال دارند:

1. مدل slot ساختاری برای کسر، رادیکال، scripts، limits و matrix؛
2. ناوبری مکانی بالا/پایین و Tab چرخه‌ای؛
3. slash-to-fraction و تایپ ساختاری؛
4. custom matrix picker از ۱×۱ تا ۱۰×۱۰؛
5. Recent با امکان حذف؛
6. کتابخانه‌های درس‌محور و بسته‌های امتحانی Smart Hub؛
7. normalization شیمی پس از اصلاح `⇌`؛
8. ویرایش مستقیم فرمول انتخاب‌شده در متن سؤال؛
9. auto-scroll خانهٔ فعال؛
10. نمایش نام نماد در کنار preview.

مواردی که نباید عیناً کپی شوند:

- پنهان‌کردن modeها و search؛
- Smart Hub بدون trigger؛
- فیلتر مبهم Unicode با عنوان 1200؛
- تعریف تکراری توابع؛
- اجرای JavaScript/WebView؛
- نمایش فرمان ناشناخته به‌صورت کد؛
- MutationObserver سراسری؛
- وابستگی ویرایش به انتخاب دستی دقیق `$...$`.

## پیوست A — تعداد ورودی هر دسته

| شناسه | عنوان | تعداد |
|---|---|---:|
| `common` | ⭐ موارد پرکاربرد | 24 |
| `basic` | ➗ پایه | 21 |
| `calc` | ∫ آنالیز | 12 |
| `trig` | ∿ مثلثات | 14 |
| `greek` | π یونانی | 18 |
| `set` | ∈ مجموعه | 20 |
| `letters` | A-Z حروف | 0 |
| `all-basic` | ➕ نمادهای پایه | 21 |
| `all-trig` | ∿ مثلثات | 10 |
| `all-algebra` | 𝑥 جبر | 10 |
| `all-geometry` | 📐 هندسه | 10 |
| `cur-num` | 🔢 محاسبات | 8 |
| `cur-number` | 🔐 نظریه اعداد | 4 |
| `cur-power` | ⁿ توان و ریشه | 5 |
| `cur-sets` | ∩ مجموعه‌ها | 5 |
| `cur-algebra` | 𝑥 عبارات جبری | 4 |
| `cur-equations` | = معادلات | 4 |
| `cur-functions` | f(x) تابع | 7 |
| `cur-matrixdet` | ▦ ماتریس/دترمینان | 5 |
| `cur-geometry` | 📐 هندسه پایه | 7 |
| `cur-solid` | ◼ هندسه فضایی | 5 |
| `cur-trigextra` | ∿ مثلثات تکمیلی | 5 |
| `cur-analytic` | 📈 هندسه تحلیلی | 5 |
| `cur-sequence` | … دنباله و سری | 4 |
| `cur-limit` | lim حد و پیوستگی | 4 |
| `cur-derivative` | ∂ مشتق | 4 |
| `cur-integral` | ∫ انتگرال | 4 |
| `cur-combin` | n! ترکیبیات | 4 |
| `cur-prob` | P احتمال | 4 |
| `cur-stats` | 📊 آمار | 5 |
| `cur-logic` | ⊢ منطق ریاضی | 7 |
| `cur-complex` | ℂ اعداد مختلط | 5 |
| `cur-vector` | → بردارها | 5 |
| `cur-linear` | ▤ جبر خطی | 6 |
| `cur-diffeq` | dy/dx دیفرانسیل | 4 |
| `cur-optimization` | ★ بهینه‌سازی | 4 |
| `cur-transform` | ℱ تبدیل‌ها | 4 |
| `cur-coordinate` | 🧭 مختصات و نمودار | 6 |
| `cur-advanced-symbols` | ⌊ نمادهای تکمیلی | 7 |
| `cur-logicsetplus` | ⊻ منطق و مجموعه پیشرفته | 7 |
| `cur-vectorcalc` | ∇ حساب برداری | 6 |
| `cur-statplus` | σ آمار پیشرفته | 5 |
| `cur-finance` | ⚙ مالی و مهندسی | 6 |
| `cur-phys-mech` | 🚀 مکانیک | 37 |
| `cur-phys-energy` | 🔥 ترمو و انرژی | 17 |
| `cur-phys-elec` | ⚡ برق و مغناطیس | 26 |
| `cur-phys-wave` | 🌊 موج و نور و مدرن | 28 |
| `cur-chem-basic` | 🧪 شیمی عمومی | 14 |
| `cur-chem-gas` | 💨 گاز و محلول | 19 |
| `cur-chem-acid` | 🧫 اسید-باز و تعادل | 18 |
| `cur-chem-thermo` | 🔥 ترموشیمی و الکتروشیمی | 19 |
| `cur-chem-organic` | 🧬 شیمی آلی و ساختار | 15 |
| `cur-physchem` | 🔬 کلید شیمی و فیزیک | 42 |
| `cur-constants` | 🔢 ثابت‌های فیزیکی | 21 |
| `cur-periodic` | 🧬 عناصر و یون‌ها | 51 |
| `cur-konkur10-phys` | 📘 فیزیک دهم | 7 |
| `cur-konkur11-phys` | 📗 فیزیک یازدهم | 11 |
| `cur-konkur12-phys` | 📕 فیزیک دوازدهم | 15 |
| `cur-konkur10-chem` | 📘 شیمی دهم | 7 |
| `cur-konkur11-chem` | 📗 شیمی یازدهم | 7 |
| `cur-konkur12-chem` | 📕 شیمی دوازدهم | 9 |
| `cur-analytic-full` | 📐 هندسهٔ تحلیلی کامل | 14 |
| `cur-tricks` | 🧠 ترفندهای ریاضی | 12 |
| `cur-units` | 📏 تبدیل واحدها | 24 |
| `cur-derivatives` | ∂ جدول مشتقات | 17 |
| `cur-integrals` | ∫ جدول انتگرال‌ها | 16 |
| `cur-matrix-formulas` | ▦ فرمول‌های ماتریس | 13 |
| `cur-series` | … سری‌های معروف | 13 |
| `cur-transforms-full` | ℱ فوریه و لاپلاس | 16 |
| `cur-phys-oscillation` | 🔁 نوسان و حرکت دوره‌ای | 12 |
| `cur-phys-optics` | 🪞 اپتیک هندسی | 13 |
| `cur-phys-waveoptics` | 🌈 اپتیک موجی | 8 |
| `cur-phys-atomic` | ⚛ فیزیک اتمی و مدرن | 15 |
| `cur-phys-extra-const` | 🔢 ثابت‌های تکمیلی | 10 |
| `cur-phys-units` | 📏 واحدهای فیزیکی | 10 |
| `matrix` | ▦ ماتریس | 3 |
| `unicode` | ⚙ یونیکد (۱۲۰۰) | 1200 |

## پیوست B — محل بخش‌های اصلی در فایل

| بخش | خط تقریبی |
|---|---:|
| CSS اصلی ویرایشگر | 6359 به بعد |
| CSS fullscreen و slot فعال | 6594–7455 |
| HTML مودال فرمول | 7699–7890 |
| موتور TeX → HTML (`math.js`) | 8883–9351 |
| Bootstrap بارگذاری تنبل | 28117–28160 |
| مدل جعبه‌ای و renderer | 28200–29100 |
| تایپ سریع و گالری | 29126–29358 |
| کتابخانه‌های اصلی | 29359–30350 |
| گروه‌های ۸گانه | 30352–30383 |
| کتابخانه/Unicode/search | 30384–30790 |
| مدیریت mode و درج در سؤال | 30789–30907 |
| TeX serializer/parser | 30908–31168 |
| علاقه‌مندی و منوهای ابزار | 31169–31410 |
| Smart Hub | 31413–31810 |
| Recent/Quick row و override نهایی | 31813–32030 |

---

**نتیجهٔ ممیزی:** بخش فرمول از نظر داده و مدل ساختاری غنی است، اما رابط runtime بخشی از امکانات تعریف‌شده را پنهان یا بدون trigger باقی گذاشته است. برای parity صحیح باید میان «قابلیت موجود در کد» و «قابلیت واقعاً قابل استفاده» تفاوت گذاشت.
