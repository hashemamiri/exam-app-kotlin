# V34 — چیدمان ابزار سؤال، Vault رمز و برش جهت‌دار/دایره‌ای

## ۱) ابزار جابه‌جایی گزینه‌ها

در کارت‌های چندگزینه‌ای و هر دو ستون جورکردنی، ترتیب ابزارها اکنون چنین است:

```text
برچسب / انتخاب پاسخ → فرمول → جابه‌جایی → تصویر → حذف (در جورکردنی)
```

بنابراین دکمهٔ drag دقیقاً کنار آیکن فرمول قرار دارد و تصویر بین آن‌ها نیست.

## ۲) تصویر متن سؤال

پس از انتخاب و تأیید تصویر، کنترل‌ها دیگر روی گوشه‌های thumbnail قرار نمی‌گیرند.
همان قرارداد تصویر گزینه استفاده شده است:

```text
تصویر ۳۰dp → مداد ۲۴dp → ضربدر قرمز ۱۷dp
```

هر سه در یک `Row` هستند و لمس تصویر همچنان نمایش تمام‌صفحه را باز می‌کند.

## ۳) رمز فعلی پس از بازکردن مجدد برنامه

با تأیید کاربر، `StudentPasswordVault` دستگاهی اضافه شد:

- رمز با کلید AES داخل `AndroidKeyStore` و حالت `AES/GCM/NoPadding` رمز می‌شود.
- کلید قابل استخراج نیست.
- `SharedPreferences` فقط IV و ciphertext احرازشده را ذخیره می‌کند.
- رکورد با شناسهٔ یکتای دانش‌آموز کلید می‌خورد، نه نام کاربری؛ بنابراین تغییر نام
  کاربری یا تکرار نام میان معلم‌ها باعث اشتباه نمی‌شود.
- `android:allowBackup="false"` از قبل فعال است؛ Vault به دستگاه دیگر منتقل نمی‌شود.
- حذف داده‌های برنامه، حذف برنامه یا invalidateشدن کلید، رمزهای محلی را از بین می‌برد.
- هیچ `plain_password`، SQL، ستون Supabase یا Vault سروری ساخته نشده است.

فقط رمزهایی که برنامه از V34 به بعد هنگام ساخت/تغییر موفق دریافت می‌کند قابل ذخیره
هستند. رمز تاریخی که فقط hash آن در Supabase موجود است، قابل بازیابی نیست و باید
یک‌بار رمز جدید برای آن تعیین شود.

دو کادر «رمز جدید» و «رمز فعلی» هر دو وزن برابر و ارتفاع دقیق ۵۶dp دارند.

## ۴) برش تصویر

### جهت کشیدن ضلع

علت رفتار نامنظم این بود که gesture حرکت کل کادر روی والد `CropFrame` قرار داشت و
روی ناحیهٔ دستگیره‌ها نیز همزمان با resize اجرا می‌شد. اکنون:

- gesture حرکت فقط در مرکز کادر با inset برابر ۲۶dp فعال است؛
- چهار دستگیره فقط resize می‌کنند؛
- `resizeDeltaForEdge` حرکت بیرونی چپ/راست/بالا/پایین را به افزایش مثبت مشترک
  تبدیل می‌کند؛
- `recenterAfterResize` لبهٔ مقابل را ثابت نگه می‌دارد و مرکز را به جهت ضلع کشیده‌شده
  انتقال می‌دهد.

### پروفایل

وقتی `forceSquare=true` (مسیر آواتار)، قاب راهنمای برش `CircleShape` و عنوان ابزار
«برش دایره‌ای پروفایل» است. فایل پایه برای سازگاری آپلود مربع باقی می‌ماند و UI
پروفایل آن را داخل قاب دایره نمایش می‌دهد.

## تست و عملیات

```text
V34BuilderVaultCropTest:
- ترتیب formula → reorder → image در چندگزینه‌ای و جورکردنی
- Row یکسان thumbnail/مداد/ضربدر
- جهت رشد هر چهار ضلع و تغییر مرکز
- قاب دایره‌ای پروفایل و جداسازی gesture مرکز از دستگیره‌ها
- AES/GCM + Android Keystore + ciphertext + allowBackup=false
- هم‌اندازه بودن دو کادر رمز

SQL جدید: ندارد
Edge Function جدید: ندارد
Migration جدید: ندارد
Dependency جدید: ندارد
پیش‌نیاز: V33
```
