# V23 — اصلاح تیک، مرکزچین کلاس و انتخاب‌گر چرخشی پایه

**تاریخ:** ۲۰۲۶-۰۸-۱۴

## دکمه ذخیره آزمون

دکمه سبز ذخیره در `Scaffold` با `FabPosition.Center` اندازه‌گیری می‌شود و دو FAB داخل یک کادر تمام‌عرض با حاشیه امن ۱۶dp قرار می‌گیرند. نویسه متنی تیک حذف و با `Icons.Outlined.Check` بومی در کادر ۵۶dp و اندازه آیکن ۲۸dp جایگزین شد؛ بنابراین baseline قلم دیگر نمی‌تواند بخشی از تیک را از کادر بیرون ببرد.

## مرکزچین کلاس

- دکمه + کلاس در یک `Column` تمام‌عرض و مرکزچین قرار دارد.
- دو کارت «افزودن موجود» و «افزودن جدید» در `Row` تمام‌عرض از مرکز و زیر + باز می‌شوند.
- انیمیشن باز/بسته‌شدن از بالای مرکز کنترل می‌شود.
- سه دکمه ورود، ویرایش و حذف داخل کارت کلاس نیز مرکزچین شدند.

## کارت دانش‌آموز و کپی

چهار کنترل کارت اکنون سطح لمس ۵۸dp و آیکن ۳۰/۳۲dp دارند:

```text
فعال/غیرفعال
ویرایش
افزودن به کلاس‌ها
کپی اطلاعات
```

کپی شامل اطلاعات قابل بازیابی است: نام کامل، نام، نام خانوادگی، نام کاربری، جنسیت، نام پدر، پایه، کلاس‌ها، وضعیت و شناسه حساب.

### محدودیت قطعی رمز

Supabase Auth رمز فعلی را فقط به‌صورت hash نگهداری می‌کند و `plain_password` نیز حذف شده است؛ در نتیجه رمز قبلی قابل خواندن یا کپی نیست. متن کپی‌شده این موضوع را صریح اعلام می‌کند. رمز فقط هنگام ساخت حساب/ساخت گروهی یا تعیین رمز جدید، همان یک‌بار قابل تحویل و ذخیره در فایل اعتبارنامه است. هیچ ذخیره‌سازی plaintext به برنامه یا دیتابیس برنگشته است.

## فیلتر افزودن موجود

- لمس دختر → فیلتر دختر.
- لمس دوباره دختر → بازگشت به همه.
- لمس پسر → فیلتر پسر.
- لمس دوباره پسر → بازگشت به همه.
- دکمه‌های جداگانه پایه حذف شدند.
- یک انتخاب‌گر عمودی شبیه کیلومترشمار در همان ردیف همه/دختر/پسر قرار گرفت.
- پیمایش بالا/پایین بین «همه پایه‌ها» و پایه‌های واقعاً موجود جابه‌جا می‌شود.

## انتخاب پایه در همه فرم‌ها

کامپوننت مشترک Native زیر تنها روش انتخاب پایه در UI است:

```text
GradeOdometerPicker
```

این کنترل در پنج محل استفاده می‌شود:

1. ساخت/ویرایش کلاس؛
2. فیلتر افزودن دانش‌آموز موجود؛
3. ویرایش دانش‌آموز؛
4. هر ردیف ساخت گروهی؛
5. پایه سربرگ رسمی معلم.

ویژگی‌ها:

- اسکرول عمودی و Snap روی گزینه مرکزی؛
- آیکن‌های Native سرعت و بالا/پایین؛
- ترتیب استاندارد پیش‌دبستانی تا دوازدهم؛
- حفظ مقدارهای سفارشی قدیمی؛
- «همه پایه‌ها» در فیلتر و «بدون پایه» در فرم‌های اختیاری؛
- توضیح دسترس‌پذیری برای پیمایش بالا/پایین.

## تست

```text
Kotlin compile                         PASS
JVM tests                              130/130 PASS
V23 interaction/grade tests            6/6 PASS
FINAL_NATIVE_VERIFY                   PASS
lintDebug                              PASS — 0 error, 22 warning
assembleDebug                         PASS
APK Signature Scheme v2               Verified
Debug APK SHA-256                     115c9c92bebd69be9f403659558292cb6c82e48e87f25045159be81a32e628a7
```

## عملیات

```text
SQL جدید: ندارد
Edge deploy: ندارد
Secret جدید: ندارد
Dependency جدید: ندارد
```
