# V27 — داده، تصویر امن، گزینه‌های زنده و پایه سایر

**تاریخ:** ۲۰۲۶-۰۸-۱۴

## واژگان سؤال

همه برچسب‌های Runtime نوع `MULTIPLE_CHOICE` از «چهارگزینه‌ای» به «چندگزینه‌ای» تغییر کردند.

## جابه‌جایی زنده گزینه‌ها

برای گزینه‌های چندگزینه‌ای و دو ستون جورکردنی شناسه‌های پایدار و محلی زیر به `QuestionDraft` اضافه شدند:

```text
optionIds
matchingLeftIds
matchingRightIds
```

این شناسه‌ها داخل Draft محلی حفظ می‌شوند ولی در Payload عمومی سرور وارد نمی‌شوند. هنگام Drag، index جاری با `rememberUpdatedState` دنبال می‌شود و با عبور از هر آستانه ۴۶dp همان لحظه `onMove(from, delta)` اجرا می‌شود. گروه‌های Compose با `key(id)` جابه‌جا می‌شوند تا gesture و state متعلق به همان گزینه باقی بماند.

## مسیر انتخاب تصویر

URI خام انتخاب‌شده دیگر به‌طور خودکار پنجره ویرایشگر را باز نمی‌کند:

- متن سؤال: همه URIها ابتدا در `LocalImageRepository.prepare` به‌صورت ترتیبی پردازش می‌شوند و فقط URI امن به ViewModel می‌رود.
- گزینه و جورکردنی: URI خام ابتدا پردازش و سپس thumbnail ثبت می‌شود؛ لمس thumbnail امن، ویرایشگر را باز می‌کند.
- پروفایل: پیش‌پردازش امن قبل از تنظیم `avatarEditing` انجام می‌شود.
- Progress و خطای ایمن در هر مسیر وجود دارد.

این تغییر مسیر خام→Dialog/Coil را حذف می‌کند. اگر بسته‌شدن روی دستگاه خاص باز هم تکرار شد، Logcat همان اجرا برای یافتن stack خارجی/سازنده لازم است و علت بدون آن اعلام نمی‌شود.

## پنجره گروهی

Surface با `Alignment.TopCenter` از بالای فضای قابل‌استفاده باز می‌شود و `height(maxHeight)` کل فضای بالای IME را می‌گیرد. `LazyColumn` وزن ۱ دارد؛ بنابراین پایین Surface همچنان با بالای کیبورد مماس است ولی محتوای اصلی در بالا قرار می‌گیرد.

## درباره و بروزرسانی

- شماره نسخه نصب‌شده با `BuildConfig.VERSION_NAME` نمایش داده می‌شود.
- یادداشت فارسی وقتی نسخه جدید وجود دارد و APK جدید هنوز دریافت نشده نمایش داده می‌شود؛ در زمان دانلود نیز باقی می‌ماند.
- پس از پایان دانلود/بروزرسانی، فهرست محلی دائمی نمایش داده نمی‌شود.

## کارت‌های منو

انتخاب کارت اکنون یک لایه رنگ accent با alpha ۱۴٪ روی کل کارت قرار می‌دهد. نشانگر خط تیره ۱۸×۵dp کاملاً حذف شد.

## داده‌ها و Storage

- کل بخش داده‌ها با `verticalScroll` قابل پیمایش است.
- دو دکمه بررسی و پاک‌سازی Storage تمام‌عرض شدند و کارت ناقص نمی‌ماند.
- دکمه واقعی «وارد کردن آزمون» اضافه شد.
- فایل حداکثر ۸MB خوانده و با `ExamPackageCodec.decode` اعتبارسنجی می‌شود.
- Draft معتبر مستقیماً به آزمون‌ساز Native منتقل می‌شود.

## پایه سایر

گزینه «سایر» به چرخ پایه اضافه شد. انتخاب آن `OutlinedTextField` با عنوان «سایر پایه» را باز می‌کند و مقدار تایپ‌شده از همان callback ذخیره می‌شود. پایه‌های استاندارد و مقدارهای سفارشی قدیمی همچنان حفظ می‌شوند.

## امنیت رمز

رمز قبلی Supabase Auth قابل بازیابی نیست و ذخیره قابل‌بازیابی جدیدی ساخته نشده است. `plain_password` همچنان حذف‌شده و ممنوع است. فقط رمز جدید ثبت‌شده با موفقیت، یک‌بار در حافظه نشست قابل نمایش و کپی حساس است.

## تست

```text
Kotlin compile                         PASS
JVM tests                              166/166 PASS
V27 data/image/options tests             9/9 PASS
FINAL_NATIVE_VERIFY                   PASS
lintDebug                              PASS — 0 error, 21 warning
assembleDebug                         PASS
APK Signature Scheme v2               Verified
Debug APK SHA-256                     23ba454a550db58422d467f121d9aa8cdb3d429f99315fdaa6088e6fd4db6e4d
```

## عملیات

```text
SQL جدید: ندارد
Edge deploy: ندارد
Secret جدید: ندارد
Dependency جدید: ندارد
```
