# V13.6 — ویرایشگر جعبه‌ای، کتابخانه فعال و رادیکال کش‌پذیر

**تاریخ:** ۲۰۲۶-۰۸-۱۲

## درخواست اصلاحی

1. تمام قسمت‌هایی که مقدار می‌گیرند یا قابل ویرایش‌اند، مانند یک جعبه دیده شوند.
2. با لمس هر جعبه، رنگ همان جعبه تغییر کند و خانهٔ فعال مشخص باشد.
3. نماد یا فرمول انتخاب‌شده از کتابخانه دقیقاً در خانهٔ فعال درج/جایگزین شود.
4. خط بالای رادیکال همراه با افزودن عدد، حرف یا نماد کشیده شود.

## پیاده‌سازی جعبه‌های واقعی

### نگاشت source به SVG

`NativeMathParser` برای هر مقدار قابل ویرایش بازهٔ واقعی آن را در TeX داخلی ثبت می‌کند:

```text
sourceStart
sourceEnd
editable
```

`NativeMathSvgRenderer` برای هر بازه مختصات زیر را تولید می‌کند:

```text
xPx / yPx / widthPx / heightPx
```

هر محدوده به یک `<rect>` واقعی در SVG تبدیل می‌شود. جعبه‌های عادی با رنگ outline پوسته و جعبهٔ فعال با رنگ primary پوسته، stroke ضخیم‌تر و fill مشخص نمایش داده می‌شوند.

### لمس و انتخاب

- `NativeFormulaEditorView` مختصات لمس را روی SVG دریافت می‌کند.
- hit-test از آخرین جعبهٔ دیداری انجام می‌شود.
- لمس جعبه، selection داخلی را دقیقاً روی بازهٔ همان مقدار قرار می‌دهد.
- تغییر selection باعث تولید SVG با رنگ فعال جدید می‌شود.
- ورودی نامرئی صفحه‌کلید از سطح کامل حذف و به `1dp` محدود شد؛ بنابراین دیگر لمس جعبه‌ها را مسدود نمی‌کند.
- فرمول خالی نیز یک خانهٔ خالی قابل لمس و رنگی دارد.
- عددها و حروف مجاور مانند `123` یک خانهٔ واحد و قابل گسترش می‌سازند.
- خانه‌های صورت/مخرج، رادیکال، توان، زیرنویس، ماتریس، متن و نماد مستقل هستند.

## اصلاح کتابخانه‌ها

`FormulaBoxEditor` مسیر مشترک و تست‌پذیر درج را فراهم می‌کند:

- اگر جعبه‌ای لمس شده باشد، انتخاب کتابخانه همان جعبه را جایگزین می‌کند.
- اگر cursor جمع‌شده باشد، جعبهٔ رنگی واقعی پیدا و کامل جایگزین می‌شود؛ درج داخل حروف فرمانی مانند `\frac` رخ نمی‌دهد.
- پس از درج قالبی مانند کسر، توان یا رادیکال، نخستین خانهٔ قابل ویرایش قالب جدید فعال می‌شود.
- تایپ keypad پس از اولین کاراکتر داخل همان خانه ادامه پیدا می‌کند.
- فلش‌های keypad میان جعبه‌ها حرکت می‌کنند و دیگر وارد byteهای کد TeX نمی‌شوند.
- حتی اگر selection سخت‌افزاری داخل بخشی از یک فرمان قرار گیرد، پیش از درج به مرز امن کل جعبه گسترش می‌یابد.
- دستهٔ فعال و گروه والد آن در UI انتخاب‌شده دیده می‌شوند.
- پنجرهٔ زیرگروه تعداد نمادهای هر دسته را نشان می‌دهد.
- ۷۷ شناسهٔ دسته یکتا هستند.
- تمام ۷۵ پیوند گروه‌ها به دستهٔ موجود اشاره می‌کنند.
- همهٔ دسته‌های غیرحروفی داده دارند و هیچ label/tex ناقص پذیرفته نمی‌شود.
- Loader در زمان اجرا ساختار کتابخانه را fail-fast اعتبارسنجی می‌کند.

### ممیزی واقعی درج

تمام موارد مرجع در یک خانهٔ نمونه درج و نتیجه بررسی شدند:

```text
ورودی دسته‌بندی‌شده + گالری: 2118/2118 PASS
پیوند دسته شکسته:                    0
دسته غیرقابل دسترسی:                 0
ورودی label/tex ناقص:                0
```

## رادیکال کش‌پذیر

طول خط بالای رادیکال از عرض layout واقعی محتوای داخل ریشه محاسبه می‌شود:

```text
barStart = محل اتصال شیب رادیکال
barEnd   = انتهای عرض کامل radicand
```

با هر تغییر در عدد، نماد، کسر، توان یا عبارت داخل رادیکال:

1. AST دوباره اندازه‌گیری می‌شود؛
2. عرض radicand جدید محاسبه می‌شود؛
3. endpoint مسیر SVG جابه‌جا می‌شود؛
4. خط بالای رادیکال تا بعد از آخرین جعبه ادامه می‌یابد.

تست کوتاه/بلند، افزایش طول bar و قرارگرفتن endpoint بعد از جعبهٔ آخر را بررسی می‌کند.

## امنیت

- TeX فقط دادهٔ داخلی است.
- SVG همچنان در حافظه و به‌صورت XML-escaped ساخته می‌شود.
- URL، شبکه، `href`, `src`, `script`, `foreignObject`, JavaScript و WebView وجود ندارند.
- جعبه‌ها فقط `rect` امن SVG هستند.
- هیچ secret، Header یا Token به رابط اضافه نشده است.

## تست نهایی

```text
Kotlin compile                         PASS
JVM tests                              73/73 PASS
Reference library insertion         2118/2118 PASS
Reference SVG generation            2118/2118 PASS
Unicode exact count                       1200 PASS
Category/link integrity                     PASS
Touch-box source ranges                     PASS
Active-box color                            PASS
Empty structural boxes                      PASS
Box-safe arrow navigation                   PASS
Radical overbar stretch                     PASS
Supplementary Unicode ranges                PASS
FINAL_NATIVE_VERIFY                         PASS
lintDebug                         PASS — 0 error, 24 warning
assembleDebug                               PASS
APK Signature Scheme v2                 Verified
```

هشدارهای lint مربوط به کدهای قبلی و اعلان نسخهٔ جدید dependencyها هستند؛ خطای lint وجود ندارد.

## عملیات استقرار

```text
SQL جدید: ندارد
Edge Function جدید: ندارد
Secret جدید: ندارد
Migration دیتابیس: ندارد
Dependency جدید: ندارد (همان SVG stack نسخه V13.5)
پیش‌نیاز: V13.5
```

## تست دستگاه پس از Build

1. فرمول خالی یک خانهٔ قابل لمس نشان دهد.
2. لمس خانهٔ صورت، مخرج، توان و رادیکال رنگ فعال را جابه‌جا کند.
3. پس از لمس خانه، نماد کتابخانه فقط همان خانه را جایگزین کند.
4. انتخاب کسر/توان/رادیکال، اولین خانهٔ قالب را فعال کند.
5. تایپ چندرقمی در یک جعبه ادامه پیدا کند.
6. فلش‌ها بین جعبه‌ها حرکت کنند و هیچ کد خامی خراب نشود.
7. رادیکال با `1`، سپس `123456789` و سپس یک کسر داخل ریشه آزمایش شود؛ خط بالا باید تا انتهای محتوا کشیده شود.
8. دسته‌های هشت‌گانه، همهٔ نمادها، Unicode، علاقه‌مندی، اخیر و گالری آزمایش شوند.
