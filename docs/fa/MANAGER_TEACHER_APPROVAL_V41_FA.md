# V41B — تأیید معلم برای تغییرات مدیر

مهاجرت `20260816_native_manager_teacher_approval_v41.sql` جدول audit درخواست‌ها، انقضای ۲۴ساعته و RPCهای تصمیم معلم را ایجاد می‌کند. حذف/ویرایش کلاس معلم در PostgreSQL و حذف/ویرایش حساب دانش‌آموز در Edge Function enforce می‌شود. تغییر عضویت کلاس خارج از این گردش‌کار است. پس از تأیید حساب دانش‌آموز، مدیر عملیات را دوباره اجرا می‌کند تا Edge Function آن را اتمیک اجرا و درخواست را `executed` کند.
