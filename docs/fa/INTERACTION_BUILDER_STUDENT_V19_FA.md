# V19 — تعامل هم‌زمان +، آزمون‌ساز شعاعی و فرم دانش‌آموز

**تاریخ:** ۲۰۲۶-۰۸-۱۳

**پیش‌نیاز:** V18

## افزودن سریع پایین برنامه

- دکمه + و سه گزینه هم‌زمان در یک انیمیشن 620ms حرکت می‌کنند.
- + از dock به مرکز می‌رود و سه کارت از مرکز در سه جهت به مقصد می‌رسند.
- دانش‌آموز بالا، آزمون راست و کلاس چپ قرار می‌گیرند.
- خطوط نقطه‌چین تا 88٪ حرکت نامرئی‌اند و فقط در 12٪ پایانی رسم می‌شوند.
- هر چهار جزء در یک لحظه به مقصد نهایی می‌رسند.
- گزینه‌ها تا پایان 96٪ حرکت قابل لمس نیستند.

## کیف پول آزمایشی امن

در حالت زیر:

```text
PAY_PROVIDER=sandbox
PAY_ALLOW_SANDBOX=true
```

دکمه «ساخت سفارش پرداخت»:

1. سفارش واقعی سروری می‌سازد؛
2. authority آزمایشی مالک همان سفارش ثبت می‌شود؛
3. Edge Function از `native_credit_wallet_payment` استفاده می‌کند؛
4. اعتبار به‌صورت اتمیک و idempotent در سرور اضافه می‌شود؛
5. WalletViewModel موجودی واقعی را بلافاصله دریافت می‌کند.

APK هیچ RPC مستقیم credit ندارد. با تغییر provider به zarinpal/idpay شاخه auto-credit کاملاً غیرفعال می‌شود.

## کارت‌های آزمون

- هر کارت در حالت بسته فقط حدود دو سطر اطلاعات فشرده دارد.
- لمس کارت، عملیات همان آزمون را با expand/fade باز می‌کند.
- هم‌زمان حداکثر یک کارت آزمون باز است.
- لمس دوباره همان کارت آزمون آن را می‌بندد؛ در آزمون‌ساز، برخلاف این صفحه، همیشه یک سؤال باز می‌ماند.

## کارت‌های مدیریتی

- فقط swipe افقی چپ/راست پذیرفته می‌شود.
- drag عمودی رد و کارت به جای خود برمی‌گردد.
- متن راهنمای «کارت را بکشید» حذف شده است.
- عنوان و توضیح کارت فعال همچنان زیر stack دیده می‌شود.

## نام برنامه

نام نمایشی Android به «آزمون آنلاین» تغییر کرد.

## افزودن دانش‌آموز

Dialog به بالای صفحه منتقل و با `imePadding` از کیبورد جدا شد.

```text
نام | نام خانوادگی
نام پدر | پایه
نام کاربری | رمز
دختر | پسر | 🎲 رمز
انصراف | ساخت حساب
```

- رمز تصادفی 10 نویسه‌ای؛
- نام کاربری از نام فارسی به قالب `first_last` پیشنهاد می‌شود؛
- پیشنهاد قابل ویرایش است و پس از ویرایش دستی دیگر overwrite نمی‌شود.

## افزودن گروهی

- textarea/CSV خام حذف و هر دانش‌آموز یک کارت ساختاریافته دارد.
- افزودن/حذف ردیف تا 100 نفر؛
- همان چیدمان فیلدهای تکی؛
- پیشنهاد Latin برای هر ردیف؛
- suffix عددی برای نام‌های تکراری؛
- کنترل تکراری‌نبودن username پیش از ارسال؛
- Dialog بالا و سازگار با کیبورد.

## فرمول

- فقط محیط ویرایش فرمول LTR شده است؛
- SVG editor از سمت چپ شروع می‌شود؛
- IME و خانه‌های جعبه‌ای LTR هستند؛
- با تغییر خانه فعال، scroll افقی/عمودی موجود خودکار خانه را داخل viewport می‌آورد؛
- نمایش نهایی سؤال و PDF بدون تغییر باقی مانده است.

## آزمون‌ساز

### مشخصات آزمون

دکمه «مشخصات آزمون» اطلاعات را expand/collapse می‌کند.

### منوی + شعاعی

+ از FAB به مرکز می‌رود، 135 درجه می‌چرخد و قرمز می‌شود. هشت گزینه هم‌زمان دور آن باز می‌شوند:

```text
تشریحی
چندگزینه‌ای
صحیح/غلط
جای خالی
عددی
جورکردنی
وارد کردن
بانک سؤال
```

- مسیر دایره‌ای نقطه‌چین فقط در انتهای انیمیشن ظاهر می‌شود؛
- وارد کردن، فایل `.azmoon/JSON` را با سقف 8MB می‌خواند؛
- بانک سؤال Dialog انتخاب باز می‌کند و سؤال انتخابی را به آزمون اضافه می‌کند.

### کارت سؤال

- همه سؤال‌ها کارت accordion هستند؛
- همیشه یک کارت باز می‌ماند؛
- با افزودن سؤال، همان کارت باز و به بالای viewport اسکرول می‌شود؛
- انتخاب کارت دیگر، کارت قبلی را می‌بندد؛
- media/formula/style/type controls فقط برای کارت باز compose می‌شوند.

### ذخیره

- دکمه ذخیره شناور با تیک سبز است؛
- لمس آن همان Dialog تأیید هزینه و ساخت/ویرایش را باز می‌کند؛
- bottom content padding برابر 112dp است تا انتهای صفحه بالای + و تیک قرار گیرد.

## Pull-to-Refresh

دکمه‌های دستی refresh از این صفحات حذف شدند:

```text
تقویم و پیام‌ها
کلاس‌ها
دانش‌آموزان
بانک سؤال
کیف پول
داشبورد آزمون‌ها
```

همه از `PullToRefreshBox` استفاده می‌کنند. در صفحه کلاس فقط محتوای کلاس و در صفحه دانش‌آموز فقط محتوای دانش‌آموز دیده می‌شود؛ جابه‌جایی از منوی همبرگری انجام می‌شود.

## تست

```text
Kotlin compile                         PASS
JVM tests                              110/110 PASS
Persian username tests                 PASS
Builder radial/accordion regression    PASS
Student single/bulk layout regression  PASS
Formula LTR/auto-scroll regression     PASS
Sandbox server-credit regression       PASS
Deno check wallet-payment              PASS
FINAL_NATIVE_VERIFY                   PASS
lintDebug                  PASS — 0 error, 22 warning
assembleDebug                         PASS
APK Signature Scheme v2               Verified
Debug APK SHA-256                     a0e9785ca749cc10f51bb8f4f708d1c3d30359290b0bcb9274bd8c25dc8631e2
```

## عملیات

```text
SQL جدید: ندارد
Migration جدید: ندارد
Secret جدید: ندارد
Dependency Android جدید: ندارد
Edge Function تغییرکرده: wallet-payment — deploy لازم است
```
