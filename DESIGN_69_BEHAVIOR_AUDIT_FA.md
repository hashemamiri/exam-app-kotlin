# ممیزی کامل رفتار و ناوبری `design-69.html`

**تاریخ ممیزی:** ۲۰۲۶-۰۸-۱۳

**SHA-256:** `8b1970f5317a6736ba20b1c239d6457e8f6739e7222d937a6d384329a95d4ecf`

**اندازه:** 61,677 بایت

**تعداد خطوط:** 467

## ۱) سلامت و ساختار

- فایل Self-contained است: یک `<style>` و یک `<script>` داخلی دارد.
- هیچ فایل، فونت، تصویر، stylesheet یا script خارجی بارگیری نمی‌شود.
- ۳۵ دکمه، ۲۱ SVG خطی، ۳۶ شناسه DOM و ۱۷ تابع JavaScript دارد.
- WebView یا JavaScript آن نباید وارد Runtime برنامه Native شود؛ این فایل فقط مرجع رفتار و ظاهر است.
- کلید، Token، Authorization، URL حساس یا credential در فایل دیده نشد.
- `prefers-reduced-motion` در مرجع وجود دارد و انیمیشن‌ها را برای کاربر حساس به حرکت تقریباً غیرفعال می‌کند.

## ۲) ماشین حالت اصلی

حالت اولیه مرجع:

```text
current = wallet
lastNonMenu = wallet
menuOpen = false
addNavExpanded = false
```

پنج مقصد نوار پایین:

```text
RTL از راست به چپ:
menu / wallet / add / exams / cards
```

رفتار عمومی تغییر صفحه:

1. صفحه قبلی غیرفعال و صفحه مقصد فعال می‌شود.
2. آیکن مقصد رنگ accent می‌گیرد، ۵px بالا می‌آید و نقطه فعال زیر آن ظاهر می‌شود.
3. برای هر صفحه انیمیشن ورود اختصاصی دوباره اجرا می‌شود.
4. Composer افزودن هنگام تغییر صفحه بسته می‌شود.
5. لمس مجدد مقصد فعال به‌جای no-op یک secondary action اجرا می‌کند.

Secondary actionها در HTML:

```text
wallet → تغییر نمونه نمودار و درصد
exams  → تغییر نمونه درصد پیشرفت
cards  → رفتن به کارت بعدی
menu/add → secondary action ندارند
```

## ۳) دکمه همبرگری و منو

منو در HTML یک Drawer کناری نیست؛ یک صفحه کامل داخل همان قاب است.

### بازشدن

- مقصد قبلی در `lastNonMenu` ذخیره می‌شود.
- افزودن سریع در صورت بازبودن بسته می‌شود.
- صفحه `menu` جای صفحه جاری را می‌گیرد.
- سه خط همبرگر با انیمیشن ۴۲۰ms به `×` تبدیل می‌شوند.
- `aria-expanded=true` و label به «بستن منو» تغییر می‌کند.

### بسته‌شدن

- لمس دوباره همان آیکن، صفحه قبلی را از `lastNonMenu` بازمی‌گرداند.
- اگر مقصد قبلی نامعتبر باشد، fallback مرجع `wallet` است.
- آیکن × دوباره همبرگر می‌شود.

### انیمیشن ورود منو

- کارت پروفایل از پایین، با overshoot کوچک، بالا می‌آید.
- کارت‌های ستون راست از چپ و کارت‌های ستون چپ از راست وارد می‌شوند.
- تأخیر کارت‌ها به‌ترتیب 120، 160، 200، 240، 280 و 320ms است.

## ۴) کارت پروفایل و شبکه منو

### پروفایل

- تمام‌عرض، حداقل ارتفاع 142px، بزرگ‌تر از همه کارت‌ها؛
- آواتار 72×72 با گوشه 25px و gradient؛
- label، نام، شناسه، تعداد کلاس و درصد پیشرفت؛
- لمس: حالت فرورفته، scale=.985 و یک bounce عمودی؛
- در نمونه فقط Toast می‌دهد، ولی در Native باید ProfileSettings واقعی باز شود.

### شش کارت مرجع

```text
تقویم              | کلاس‌ها
دانش‌آموزان        | درباره و بروزرسانی
تنظیمات             | خروج
```

- دو ستون و gap=15px؛
- هر کارت حداقل 116px؛
- آیکن 36×36 در سطح فرورفته؛
- لمس: inner shadow و scale=.97؛
- کارت خروج قرمز است.

رفتارهای نمونه:

```text
تقویم/کلاس/دانش‌آموز → فقط Toast نمایشی
بروزرسانی             → check سبز + تغییر متن به آخرین نسخه نصب است
تنظیمات                → اسکرول صفحه Web به control panel بیرون دستگاه
خروج                    → لمس اول arm؛ لمس دوم تا 3.5s خروج آزمایشی
```

## ۵) نوار پایین و آیکن‌ها

- dock ارتفاع 80px، حاشیه افقی 16px، گوشه 28px و blur پس‌زمینه دارد.
- دکمه معمولی 55×55 و radius=19px است.
- دکمه فعال 5px بالا می‌رود، inner shadow می‌گیرد و نقطه 4px زیر آن ظاهر می‌شود.
- هر لمس ripple دایره‌ای 520ms دارد.

میکروانیمیشن آیکن‌ها:

```text
menu   → فشرده/بازشدن؛ هنگام منو morph به ×
wallet → flip افقی 180 درجه
exams  → bounce عمودی
cards  → wiggle افقی + rotation
```

## ۶) دکمه + و صفحه افزودن

### دکمه dock

- دایره کامل 67×67؛
- 27px بالاتر از dock قرار می‌گیرد؛
- gradient و سایه raised دارد.

### بازشدن از dock

1. منو در صورت بازبودن بسته می‌شود.
2. صفحه `add` فعال می‌شود.
3. مختصات مرکز صفحه محاسبه می‌شود.
4. همان دکمه + از dock تا مرکز صفحه حرکت می‌کند.
5. هم‌زمان 135 درجه می‌چرخد و به × تبدیل می‌شود.
6. پس از 360ms سه گزینه با خطوط dashed و stagger باز می‌شوند.

### گزینه‌های مرجع

```text
بالا: تراکنش
پایین راست: کارت جدید
پایین چپ: آزمون
```

هر گزینه صفحه composer پایین را باز می‌کند. Composer مرجع فقط placeholder و دکمه «ثبت پیش‌نویس» دارد و داده واقعی ذخیره نمی‌کند.

## ۷) کیف پول

- کارت gradient موجودی، دکمه چشم و دو حباب تزئینی؛
- چشم موجودی را بین عدد و `••••••••` عوض می‌کند؛
- سه عملیات واریز/انتقال/دریافت در نمونه فقط Toast هستند؛
- لمس مجدد تب Wallet یکی از سه نمودار و درصد نمونه را cycle می‌کند؛
- مقادیر و تراکنش‌های HTML ساختگی‌اند و نباید به Runtime Native منتقل شوند.

## ۸) آزمون‌ها

- overview با حلقه پیشرفت و streak؛
- دکمه refresh بالای صفحه مقدارهای نمونه 66/82/94 را cycle می‌کند؛
- لمس هر آزمون آن را active و متن را «درحال اجرا» می‌کند؛
- Toast شروع آزمون 1600ms؛
- داده‌ها نمونه‌اند و با TeacherDashboard/StudentExam واقعی یکسان نیستند.

## ۹) کارت‌ها

HTML در این بخش کارت بانکی نمایش می‌دهد، نه کارت مدیریتی V15 سامانه آزمون.

- سه کارت bank-style روی هم؛
- drag در چهار جهت با Pointer Events؛
- آستانه پذیرش 52px؛
- جهت غالب افقی/عمودی تعیین می‌شود؛
- کارت پذیرفته‌شده از قاب خارج و کارت بعد/قبل فعال می‌شود؛
- drag لغوشده طی 320ms به مبدا بازمی‌گردد؛
- Arrow keys نیز کارت بعد/قبل را عوض می‌کنند؛
- دکمه بعدی، مسدودسازی، رمز پویا و جزئیات دارد؛
- همه داده‌ها و عملیات در HTML نمایشی‌اند.

## ۱۰) تنظیم ظاهر

Control panel بیرون قاب موبایل است و شامل:

- چهار پالت؛
- depth از 8 تا 28؛
- بازنشانی به پالت اول و depth=18؛
- حذف قاب دستگاه که فقط برای نمایش Web است.

در Native، palette/depth واقعی V16 موجود است. «حذف قاب» کاربرد Runtime ندارد.

## ۱۱) تعارض‌های مهم با برنامه Native فعلی

```text
HTML menu     = صفحه کامل و بازگشت به lastNonMenu
Native V16    = Modal Drawer از سمت راست

HTML +        = تراکنش / کارت بانکی / آزمون در صفحه اختصاصی
Native V15/16 = دانش‌آموز / آزمون / کلاس در کمان روی صفحه فعلی

HTML cards    = کارت بانکی قابل swipe
Native V15/16 = آمار / تصحیح / مانده با مسیرهای مدیریتی واقعی

HTML exams    = شروع آزمون دانش‌آموز و درصد نمایشی
Native teacher= مدیریت آزمون‌های واقعی

HTML logout   = double-tap ظرف 3.5 ثانیه
Native        = AlertDialog تأیید خروج

HTML update   = check نمایشی داخل کارت
Native        = AboutScreen + بررسی و دانلود واقعی APK

HTML wallet   = واریز/انتقال/دریافت نمایشی
Native        = موجودی/شارژ امن/تراکنش واقعی
```

این تعارض‌ها بدون تصمیم کاربر نباید با حدس حل شوند.

## ۱۲) تصمیم‌های تأییدشده کاربر برای Native

```text
بازشدن منو:              صفحه کامل؛ بازگشت به آخرین صفحه
دکمه +:                  حرکت به مرکز و مثلث؛ عملیات واقعی دانش‌آموز/آزمون/کلاس
کارت‌ها:                 stack مدیریتی واقعی با swipe چهارجهته
تعداد منو:               همه مسیرهای واقعی؛ ۱۰ معلم و ۶ دانش‌آموز
بروزرسانی/خروج:          AboutScreen واقعی + AlertDialog امن
آیکن و حرکت:             خطی Native، morph/flip/bounce/wiggle/ripple/stagger
لمس دوباره مقصد فعال:   refresh داده واقعی یا cycle کارت مدیریتی
```

داده‌های نمونه HTML، کارت بانکی ساختگی، موجودی ثابت، درصدهای ساختگی و JavaScript وارد Runtime نمی‌شوند.
