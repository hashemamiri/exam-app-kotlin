# V24 — تقویم، منو، آزمون‌ساز و ویرایش تصویر

**تاریخ:** ۲۰۲۶-۰۸-۱۴

## امنیت رمز دانش‌آموز

ذخیره یا بازیابی رمز قبلی انجام نشده است. Supabase Auth رمز را به‌صورت hash یک‌طرفه نگه می‌دارد و `plain_password` نیز همچنان حذف است. بازگرداندن ذخیره‌سازی رمز قابل‌بازیابی، امنیت حساب دانش‌آموزان، فایل پشتیبان و دیتابیس را نقض می‌کرد.

جایگزین امن V24:

- فیلد ویرایش همچنان «رمز جدید اختیاری» است؛
- رمز فعلی صریحاً غیرقابل نمایش اعلام می‌شود؛
- اگر معلم رمز جدید تعیین کند و عملیات سرور موفق شود، همان رمز فقط در حافظه نشست و یک‌بار نمایش داده می‌شود؛
- دکمه «کپی اطلاعات و رمز جدید» Clipboard را با برچسب حساس Android پر می‌کند؛
- با بستن پنجره، مقدار از state حذف می‌شود؛
- دکمه کپی عادی کارت همه مشخصات و نام کاربری را کپی می‌کند، اما رمز قبلی را جعل یا بازیابی نمی‌کند.

## تقویم

- جمعه در شبکه همچنان قرمز است.
- عبارت «تعطیل: جمعه» از کارت پیام‌های روز حذف شد.
- اگر مناسبت رسمی دیگری وجود داشته باشد عنوان واقعی آن نمایش داده می‌شود.

## منوی همبرگری

- انیمیشن‌های stagger و slide تو‌در‌تو حذف شدند.
- بازشدن پوسته به fade کوتاه ۱۱۰ms و morph آیکن به ۱۸۰ms کاهش یافت.
- تعداد کارت‌های معلم ۸ و دانش‌آموز ۶ شد.
- «حساب» و «داده‌ها» به مسیرهای مستقل منو تبدیل شدند.
- ترتیب معلم: دانش‌آموزان، کلاس‌ها، تقویم، سربرگ، حساب، داده‌ها، تنظیمات، خروج.
- تنظیمات فقط «ظاهر» و «درباره» را نگه می‌دارد.

## حساب

کارت‌های مشخصات، نام کاربری، ایمیل، رمز جدید و قفل برنامه مستقل‌اند. لمس سربرگ هر کارت آن را باز می‌کند و لمس دوباره می‌بندد.

## درباره و بروزرسانی

توضیحات فنی و کارت‌های اضافی حذف شدند. صفحه فقط فهرست فارسی تغییرات نسخه‌های V18 تا V24 و کنترل‌های کوتاه بررسی/دریافت بروزرسانی را نمایش می‌دهد.

## انتخاب پایه

کنترل قبلی کیلومترشمار حذف و به یک فیلد جمع‌وجور تبدیل شد. لمس فیلد، پنجره چرخ عمودی پنج‌ردیفه با Snap، گزینه مرکزی برجسته و تیک/انصراف را باز می‌کند. ترتیب استاندارد و مقدارهای سفارشی قبلی حفظ می‌شوند.

## بازه آزمون

- «شروع» و «پایان» در یک سطر هستند.
- تعیین‌شده سبز و تعیین‌نشده قرمز است.
- پنجره دارای تقویم شمسی واقعی، تغییر ماه و انتخاب روز است.
- ساعت در چپ و دقیقه در راست قرار دارد.
- تیک سبز برای ثبت دستی، «اکنون» برای زمان دستگاه و ضربدر قرمز برای انصراف در مرکز هستند.

## مشخصات و کارت سؤال

- تصادفی‌سازی سؤال‌ها و گزینه‌ها دو chip در یک سطر هستند و انتخاب‌شده با متن Bold دیده می‌شود.
- فلش‌های جابه‌جایی حذف شدند.
- نگه‌داشتن DragIndicator و کشیدن بالا/پایین، سؤال را جابه‌جا و شماره را زنده بازشماری می‌کند.
- شماره فقط به‌شکل عدد در حلقه نئونی ملایم نمایش داده می‌شود.
- شماره، نوع، بارم، چشم و DragIndicator در یک سطر هستند.
- بارم پیش‌فرض placeholder دارد و با لمس قابل ورود است.
- چشم، بخش چیدمان/ظاهر چاپ و پیش‌نمایش همان سؤال را باز و بسته می‌کند.
- حساسیت به حروف chip مرکزچین و Bold شونده است.

## تصاویر

- تصاویر مدیریت‌شده در `LazyRow` کنار هم قرار می‌گیرند.
- مختصات اولیه تصاویر جدید نیز شبکه‌ای و بدون هم‌پوشانی تعیین می‌شود.
- بوم آزاد فقط وقتی جای تصویر «آزاد» است جداگانه نمایش داده می‌شود.
- ویرایشگر مشترک همه تصاویر فقط چرخش چپ، برش و چرخش راست دارد.
- برش یک کادر مربع دارد؛ داخل کادر قابل جابه‌جایی و هر چهار ضلع قابل Drag است.
- پایین پنجره تیک سبز، حجم تقریبی زنده و ضربدر قرمز در یک سطر قرار دارند.

## تست

```text
Kotlin compile                         PASS
JVM tests                              138/138 PASS
V24 comprehensive UX tests              8/8 PASS
FINAL_NATIVE_VERIFY                   PASS
lintDebug                              PASS — 0 error, 21 warning
assembleDebug                         PASS
APK Signature Scheme v2               Verified
Debug APK SHA-256                     1e6b84f426b2395b886ad9d7271547c9273f613e686bf3649e285759c78a3fec
```

## عملیات

```text
SQL جدید: ندارد
Edge deploy: ندارد
Secret جدید: ندارد
Dependency جدید: ندارد
```
